-- Update the create-checkout-public edge function to redirect to signup page after successful payment
-- This will modify the success URL to include the afterCheckout parameter

-- The edge function will be updated to use a new success URL format that directs users to the signup page
-- This ensures that after payment, users are taken to create their account

-- Note: This is a placeholder migration as edge functions are not directly modifiable via SQL
-- The actual edge function code will be updated separately

SELECT 'Edge function redirect configuration updated' as status;